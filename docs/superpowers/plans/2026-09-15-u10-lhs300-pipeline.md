# ellipsoid_u10 LHS-300 流水线实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 全部重采 ~300 个 LHS latent 形状，固定 BC `[10,1,0,0]` 12 并发批量生成 OpenFOAM 快照，然后链式训练 PiPOD-DeepONet stage 1-3。

**Architecture:** 单个 bash 驱动脚本 `run_u10_pipeline.sh`（按 `0|1|2|3|all` 选择阶段），复用现有 `generate_openfoam_snapshots.py` / `train_pipod_deeponet.py`，零 Python 代码改动。首个任务先用 uv 重建项目 `.env`（本机 torch/foamlib 环境已不存在）。

**Tech Stack:** bash、Python 3.12 venv（uv）、torch 2.6.0+cu126、OpenFOAM 14、foamlib。

**Spec:** `docs/superpowers/specs/2026-09-15-u10-lhs300-pipeline-design.md`

## Global Constraints

- 仓库 **不是 git 仓库**：所有 Commit 步骤跳过。
- Python 一律用 `$REPO_ROOT/.env/bin/python`；系统 python3 无 torch/foamlib，禁止直接用 `python3`。
- 快照/训练网格必须一致：生成端 `--grid_stretch` ↔ 训练端 `pipod_config.json` 的 `grid_stretch: true`（默认已是）。
- 训练配置只允许改两处：`iters: 20000`、`n_field: 60000`，其余保持原值。
- LHS 采样参数固定：`--lhs 310 --seed 3`（310 预留 ~2% 拒绝余量，目标 ~300 接受）。
- 批量参数固定：`--fixed_bc 10 1 0 0 --jobs 12 --grid_stretch`，root 固定 `data/openfoam/ellipsoids_u10`。
- 实验目录固定 `examples/ellipsoids_of4`；OpenFOAM 环境 `source /opt/openfoam14/etc/bashrc`（其 bashrc 与 `set -u` 冲突，需临时 `set +u`）。
- 每个 OpenFOAM case 单核（`--n_procs` 默认 1），12 并发共 12 核（机器 256 核）。

---

### Task 1: 创建 .env Python 环境

**Files:**
- Create: `/home/s/CASES/deepSDF_v2/.env/`（uv venv，不入版本控制）

**Interfaces:**
- Consumes: uv（`/home/s/paraview/bin/uv`，已在 PATH）；PyPI 与外网（已验证 HTTP 200）。
- Produces: `.env/bin/python`，含 torch（CUDA、arch 含 sm_70）+ foamlib + plyfile + scikit-image + trimesh + sympy + numpy + scipy；后续所有任务的 `PY` 指向它。

- [ ] **Step 1: 创建 venv 并安装 torch（cu126，含 V100 的 sm_70）**

```bash
cd /home/s/CASES/deepSDF_v2
uv venv --python 3.12 .env
uv pip install --python .env/bin/python \
    --index-url https://download.pytorch.org/whl/cu126 \
    "torch==2.6.0"
```

- [ ] **Step 2: 安装其余依赖**

```bash
cd /home/s/CASES/deepSDF_v2
uv pip install --python .env/bin/python \
    foamlib plyfile scikit-image trimesh sympy numpy scipy
```

- [ ] **Step 3: 验证 CUDA arch 与关键导入**

```bash
cd /home/s/CASES/deepSDF_v2
.env/bin/python - <<'EOF'
import torch
assert torch.cuda.is_available(), "CUDA not available"
archs = torch.cuda.get_arch_list()
print("arch list:", archs)
assert any("70" in a for a in archs), "sm_70 missing (V100 unsupported)"
import foamlib, plyfile, skimage, trimesh, sympy, scipy
print("deps OK")
EOF
```

预期：打印 arch list（含 `sm_70`）与 `deps OK`，退出码 0。

- [ ] **Step 4: 验证项目脚本可导入**

```bash
cd /home/s/CASES/deepSDF_v2
.env/bin/python -c "import generate_openfoam_snapshots; import train_pipod_deeponet; print('project imports OK')"
```

预期：打印 `project imports OK`（这两个 import 会连锁拉入 deep_sdf.*、networks.*、reconstruct 等全部模块）。

---

### Task 2: 编写驱动脚本 run_u10_pipeline.sh

**Files:**
- Create: `/home/s/CASES/deepSDF_v2/run_u10_pipeline.sh`

**Interfaces:**
- Consumes: Task 1 的 `.env/bin/python`；`generate_openfoam_snapshots.py`、`train_pipod_deeponet.py`、`examples/ellipsoids_of4/pipod_config.json`、`/opt/openfoam14/etc/bashrc`。
- Produces: 可执行脚本，子命令 `0|1|2|3|all`；Task 3-6 全部通过它执行。日志写到 `data/openfoam/ellipsoids_u10/pipeline_<时间戳>.log`。

- [ ] **Step 1: 写入完整脚本**

```bash
#!/usr/bin/env bash
# ellipsoid_u10 LHS-300 pipeline driver.
# Spec: docs/superpowers/specs/2026-09-15-u10-lhs300-pipeline-design.md
#
# Usage: ./run_u10_pipeline.sh [0|1|2|3|all]   (default: all)
#   0 = env-independent prep + smoke test (prep is idempotent)
#   1 = LHS 310 re-sample + OpenFOAM batch (--jobs 12)
#   2 = update pipod_config.json (iters=20000, n_field=60000)
#   3 = PiPOD-DeepONet stage 1->2->3 (chained init_from)
set -euo pipefail

REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$REPO_ROOT"

PY="$REPO_ROOT/.env/bin/python"
[ -x "$PY" ] || { echo "missing $PY (run Task 1 first)" >&2; exit 1; }

DATA_ROOT="data/openfoam/ellipsoids_u10"
SMOKE_ROOT="data/openfoam/_smoke_u10"
EXPERIMENT="examples/ellipsoids_of4"
CONFIG="$EXPERIMENT/pipod_config.json"
OF_BASHRC="/opt/openfoam14/etc/bashrc"
LOG="$DATA_ROOT/pipeline_$(date +%Y%m%d_%H%M%S).log"

mkdir -p "$DATA_ROOT"
exec > >(tee -a "$LOG") 2>&1
echo "[$(date '+%F %T')] log: $LOG ($(realpath "$LOG"))"

# OpenFOAM 14 env (its bashrc trips 'set -u', relax around it)
set +u
source "$OF_BASHRC"
set -u

STAGE="${1:-all}"

stage0() {
    echo "[$(date '+%F %T')] === Stage 0: prep + smoke ==="
    # 移走旧 manifest（全新重采；保留备份防误删）
    if [ -f "$DATA_ROOT/lhs_latents.npz" ]; then
        mv "$DATA_ROOT/lhs_latents.npz" "$DATA_ROOT/lhs_latents.npz.pre300.bak"
        echo "old manifest -> lhs_latents.npz.pre300.bak"
    fi
    # 清理上次被 kill 的 4 个半成品 case
    rm -rf "$DATA_ROOT"/cases/lhs_shape_00{0,1,2,3}_case000
    # 冒烟：临时 root 跑 4 采样/2 并发，验证 mesh->solver->postProcess 全链路
    rm -rf "$SMOKE_ROOT"
    "$PY" generate_openfoam_snapshots.py --root "$SMOKE_ROOT" \
        --lhs 4 --seed 99 --fixed_bc 10 1 0 0 --jobs 2 --grid_stretch \
        --experiment "$EXPERIMENT" --data_source data/ellipsoids
    n=$(find "$SMOKE_ROOT/snapshots" -name '*.npz' | wc -l)
    rm -rf "$SMOKE_ROOT"
    if [ "${n// /}" -lt 1 ]; then
        echo "SMOKE TEST FAILED: no snapshot produced" >&2
        exit 1
    fi
    echo "smoke test ok (${n// /} snapshots)"
}

stage1() {
    echo "[$(date '+%F %T')] === Stage 1: LHS 310 + OpenFOAM batch (jobs=12) ==="
    "$PY" generate_openfoam_snapshots.py --root "$DATA_ROOT" \
        --lhs 310 --seed 3 --fixed_bc 10 1 0 0 --jobs 12 --grid_stretch \
        --experiment "$EXPERIMENT" --data_source data/ellipsoids
    # 有 failed case 则中止（训练要求 manifest 每个形状至少一个快照）
    "$PY" - <<'EOF'
import json, sys
s = json.load(open("data/openfoam/ellipsoids_u10/batch_summary.json"))
print("batch:", s["cases"])
failed = s["cases"]["failed"]
if failed:
    print("FAILED CASES: " + ", ".join(f["case"] for f in failed), file=sys.stderr)
    sys.exit(1)
EOF
}

stage2() {
    echo "[$(date '+%F %T')] === Stage 2: update pipod_config.json ==="
    "$PY" - <<'EOF'
import json
p = "examples/ellipsoids_of4/pipod_config.json"
cfg = json.load(open(p))
cfg["iters"] = 20000
cfg["n_field"] = 60000
with open(p, "w") as f:
    json.dump(cfg, f, indent=2)
    f.write("\n")
print("iters =", cfg["iters"], "| n_field =", cfg["n_field"])
EOF
}

stage3() {
    for st in 1 2 3; do
        echo "[$(date '+%F %T')] === Stage 3: train stage $st ==="
        "$PY" train_pipod_deeponet.py --config "$CONFIG" --stage "$st"
    done
}

case "$STAGE" in
    0) stage0 ;;
    1) stage1 ;;
    2) stage2 ;;
    3) stage3 ;;
    all) stage0; stage1; stage2; stage3 ;;
    *) echo "usage: $0 [0|1|2|3|all]" >&2; exit 2 ;;
esac
echo "[$(date '+%F %T')] stage '$STAGE' finished OK"
```

- [ ] **Step 2: 赋执行权限 + 语法检查**

```bash
cd /home/s/CASES/deepSDF_v2
chmod +x run_u10_pipeline.sh
bash -n run_u10_pipeline.sh && echo "syntax OK"
```

预期：打印 `syntax OK`。

- [ ] **Step 3: 帮助分支冒烟**

```bash
cd /home/s/CASES/deepSDF_v2
./run_u10_pipeline.sh badarg; echo "exit=$?"
```

预期：打印 usage 且 `exit=2`（验证 case 分支与 `set -e` 行为正常）。

---

### Task 3: 执行 Stage 0（预备 + 冒烟测试）

**Files:**
- Modify（由脚本）: 备份 `data/openfoam/ellipsoids_u10/lhs_latents.npz`；删除 4 个残留 case 目录。

**Interfaces:**
- Consumes: Task 2 的脚本。
- Produces: `lhs_latents.npz.pre300.bak`；确认 mesh/solver/postProcess 全链路在当前机器可用；后续 Stage 1 的前提。

- [ ] **Step 1: 运行 Stage 0**

```bash
cd /home/s/CASES/deepSDF_v2
./run_u10_pipeline.sh 0
```

预期：日志显示 old manifest 移走、冒烟运行（几分钟内完成）、`smoke test ok (N snapshots)`（N≥1，4 采样中网格有效性过滤后通常 N=2~4）。若 snappyHexMesh/simpleFoam 失败，脚本以非零退出——不要继续后续任务，按 log 排查（`_smoke_u10` 目录在失败时被保留... 实际成功路径才删除；失败时 `set -e` 中断，`rm -rf "$SMOKE_ROOT"` 未执行，可检查 `data/openfoam/_smoke_u10/cases/` 内失败 case 的 log.*）。

- [ ] **Step 2: 验证产物状态**

```bash
cd /home/s/CASES/deepSDF_v2
ls data/openfoam/ellipsoids_u10/lhs_latents.npz.pre300.bak
ls data/openfoam/ellipsoids_u10/cases/ 2>/dev/null | wc -l   # 应为 0（无残留）
ls data/openfoam/_smoke_u10 2>/dev/null || echo "smoke root cleaned"
```

预期：备份文件存在；cases 数 0；smoke root 已清理。

---

### Task 4: 更新 pipod_config.json（Stage 2）

**Files:**
- Modify: `examples/ellipsoids_of4/pipod_config.json`（`iters`、`n_field` 两个键）

**Interfaces:**
- Consumes: Task 2 脚本的 `stage2`。
- Produces: 更新后的配置，供 Task 6 训练使用。

- [ ] **Step 1: 运行 Stage 2**

```bash
cd /home/s/CASES/deepSDF_v2
./run_u10_pipeline.sh 2
```

预期：打印 `iters = 20000 | n_field = 60000`。

- [ ] **Step 2: 验证配置内容**

```bash
cd /home/s/CASES/deepSDF_v2
.env/bin/python - <<'EOF'
import json
cfg = json.load(open("examples/ellipsoids_of4/pipod_config.json"))
assert cfg["iters"] == 20000, cfg["iters"]
assert cfg["n_field"] == 60000, cfg["n_field"]
assert cfg["grid_stretch"] is True
assert cfg["latent_manifest"] == "data/openfoam/ellipsoids_u10/lhs_latents.npz"
assert cfg["snapshots"] == "data/openfoam/ellipsoids_u10/snapshots"
assert [s["stage"] for s in cfg["stages"]] == [1, 2, 3]
print("config OK")
EOF
```

预期：打印 `config OK`。

---

### Task 5: 启动 Stage 1 批量生成（长时间后台任务）

**Files:**
- Create（由脚本）: `data/openfoam/ellipsoids_u10/lhs_latents.npz`、`snapshots/*.npz`（~332 个）、`batch_summary.json`

**Interfaces:**
- Consumes: Task 3 完成的 prep、Task 4 完成与否无关（训练才用配置）。
- Produces: 全新 manifest（27 椭球 + ~300 LHS）+ 全量快照，供 Task 6 训练。

- [ ] **Step 1: 后台启动 Stage 1**

用 run_in_background 启动（预计 2.5~4 小时）：

```bash
cd /home/s/CASES/deepSDF_v2
./run_u10_pipeline.sh 1
```

（后台任务，disable_timeout 或 timeout 14400。）

- [ ] **Step 2: 启动验证（前 10 分钟内检查）**

```bash
cd /home/s/CASES/deepSDF_v2
ls -t data/openfoam/ellipsoids_u10/pipeline_*.log | head -1   # 找到当前日志
tail -5 "$(ls -t data/openfoam/ellipsoids_u10/pipeline_*.log | head -1)"
ls data/openfoam/ellipsoids_u10/cases/ 2>/dev/null | wc -l    # 应在 0~24 之间增长
ls data/openfoam/ellipsoids_u10/snapshots/*.npz 2>/dev/null | wc -l  # 逐步增长
```

预期：日志出现 `LHS: 310 samples ...`、`training latents: 27 codes`、`case N/M ... ok`；cases 目录并发数 ≤12；快照数单调增长。若 15 分钟内无任何 case 完成且日志卡在 mesh 阶段，打开对应 `cases/<name>/log.snappyHexMesh` 排查。

- [ ] **Step 3: 等待完成并核对结果**

等待后台任务结束（自动通知）。完成后：

```bash
cd /home/s/CASES/deepSDF_v2
.env/bin/python - <<'EOF'
import json
s = json.load(open("data/openfoam/ellipsoids_u10/batch_summary.json"))
print("shapes:", s["shapes"]["ellipsoids"], "+ LHS", s["shapes"]["lhs_accepted"],
      "| rejected", len(s["shapes"]["lhs_rejected"]))
print("cases:", s["cases"])
assert not s["cases"]["failed"], s["cases"]["failed"]
EOF
ls data/openfoam/ellipsoids_u10/snapshots/*.npz | wc -l
```

预期：27 + ~300 LHS（接受数 ≥295）、failed 为空（否则驱动脚本已非零退出——修复后重跑 `./run_u10_pipeline.sh 1`，已完成的快照自动跳过）；快照总数 ~332（含 of4_view）。

---

### Task 6: 启动 stage 1-3 训练（长时间后台任务）

**Files:**
- Create（由脚本）: `examples/ellipsoids_of4/PipodONet/stage{1,2,3}.pth`、`metrics_stage{1,2,3}.jsonl`、`config_stage{1,2,3}.json`

**Interfaces:**
- Consumes: Task 5 的全量快照 + manifest；Task 4 更新后的配置。
- Produces: 三个阶段的 checkpoint 与指标日志（覆盖旧文件）。

- [ ] **Step 1: 后台启动 Stage 3（训练）**

```bash
cd /home/s/CASES/deepSDF_v2
./run_u10_pipeline.sh 3
```

（后台任务。stage1 约 20000 iter 较快；stage2/3 各 20000 iter 且 n_field=60000 + stage3 二阶物理残差，预计数小时。）

- [ ] **Step 2: 启动验证（前 10 分钟）**

```bash
cd /home/s/CASES/deepSDF_v2
tail -5 examples/ellipsoids_of4/PipodONet/metrics_stage1.jsonl 2>/dev/null
nvidia-smi --query-gpu=index,utilization.gpu,memory.used --format=csv
```

预期：stage1 的 jsonl 开始写入记录；GPU0 利用率 >0。同时观察日志确认 `latent manifest ... 33X shapes` 与 `snapshot ensemble: N train / M val cases`（M≈66，N≈266）。

- [ ] **Step 3: 等待三阶段完成**

等待后台任务结束。每阶段完成后日志出现 `stage-N training time: ...; saved .../stageN.pth`。

---

### Task 7: 训练结果验证

**Files:**
- Read: `examples/ellipsoids_of4/PipodONet/stage{1,2,3}.pth`、`metrics_stage{1,2,3}.jsonl`

**Interfaces:**
- Consumes: Task 6 产物。

- [ ] **Step 1: 核对 checkpoint 与最终指标**

```bash
cd /home/s/CASES/deepSDF_v2
ls -la examples/ellipsoids_of4/PipodONet/stage{1,2,3}.pth
for n in 1 2 3; do
  echo "=== stage $n final ==="
  tail -1 examples/ellipsoids_of4/PipodONet/metrics_stage$n.jsonl
done
```

预期：三个 .pth 存在且为最新时间戳；每行 jsonl 末行含 `"final": true` 与 `best_val_rel_l2`。stage2/3 的 `val_rel_l2` 应明显低于 mean-field baseline（checkpoint 内 `val_mean_field_rel_l2`）。

- [ ] **Step 2: （可选）可视化指标曲线**

```bash
cd /home/s/CASES/deepSDF_v2
.env/bin/python plot_pipod_metrics.py 2>&1 | tail -3 || echo "plot script skipped"
```

---

## Self-Review 记录

- **Spec 覆盖**：spec 的 Stage 0（含新增的环境准备）→ Task 1+3；Stage 1 → Task 5；Stage 2 → Task 4；Stage 3 → Task 6；错误处理（failed 中止、set -e）→ 脚本内实现 + Task 5 Step 3 断言；日志 → 脚本 `exec > >(tee ...)`。全覆盖。
- **占位符**：无 TBD/TODO；脚本与命令均为完整可执行内容。
- **一致性**：脚本子命令 `0|1|2|3|all` 与各 Task 调用一致；`PY` 路径、root、experiment 路径全文统一；Task 5 的快照数 ~332 = 27+~305 LHS+of4_view，与 spec 一致。
- **已修正**：Task 1 为 spec 阶段之后新增（环境缺失是在 writing-plans 调研中发现的阻塞，spec 已同步补充 Stage 0 第 0 步）。
